#include <sys/syscall.h>
#include <unistd.h>

int main() {
    // Deprecated; expect failure but exercise dispatch.
    long res = syscall(SYS_query_module, "nonexistent", 0, 0, 0, 0);
    (void)res;
    return 0;
}
