#include <signal.h>
#include <sys/syscall.h>
#include <unistd.h>

int main() {
    sigset_t set;
    sigemptyset(&set);
    return syscall(SYS_rt_sigprocmask, SIG_SETMASK, &set, 0, sizeof(sigset_t)) == 0 ? 0 : 1;
}
