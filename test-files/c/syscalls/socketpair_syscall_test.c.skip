#include <sys/socket.h>
#include <sys/syscall.h>
#include <unistd.h>

int main() {
    int fds[2];
    syscall(SYS_socketpair, AF_UNIX, SOCK_STREAM, 0, fds);
    if (fds[0]) syscall(SYS_close, fds[0]);
    if (fds[1]) syscall(SYS_close, fds[1]);
    return 0;
}
