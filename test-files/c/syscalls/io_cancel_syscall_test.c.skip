#include <sys/syscall.h>
#include <unistd.h>

int main() {
    aio_context_t ctx = 0;
    syscall(SYS_io_setup, 1, &ctx);
    long res = syscall(SYS_io_cancel, ctx, 0, 0);
    syscall(SYS_io_destroy, ctx);
    return res == 0 || res < 0 ? 0 : 1;
}
