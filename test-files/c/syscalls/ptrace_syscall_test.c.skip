#include <sys/syscall.h>
#include <unistd.h>

int main() {
    // Intentionally use invalid arguments; success here means syscall dispatched.
    return syscall(SYS_ptrace, 0, 0, 0, 0) < 0 ? 0 : 1;
}
