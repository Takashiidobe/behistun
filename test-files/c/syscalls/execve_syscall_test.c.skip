#include <sys/syscall.h>
#include <unistd.h>

extern char **environ;

int main() {
    char *const argv[] = {"/bin/true", NULL};
    syscall(SYS_execve, "/bin/true", argv, environ);
    return 1; // Only reached if execve fails.
}
