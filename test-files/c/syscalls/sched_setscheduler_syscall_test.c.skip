#include <sched.h>
#include <sys/syscall.h>
#include <unistd.h>

int main() {
    struct sched_param p = {.sched_priority = 0};
    long res = syscall(SYS_sched_setscheduler, 0, SCHED_OTHER, &p);
    return res == 0 || res < 0 ? 0 : 1;
}
