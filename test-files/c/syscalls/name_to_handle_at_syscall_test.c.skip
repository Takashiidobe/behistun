#include <sys/syscall.h>
#include <linux/openat2.h>
#include <unistd.h>

int main() {
    struct file_handle fh = {0};
    int mount_id = 0;
    syscall(SYS_name_to_handle_at, AT_FDCWD, ".", &fh, &mount_id, 0);
    return 0;
}
