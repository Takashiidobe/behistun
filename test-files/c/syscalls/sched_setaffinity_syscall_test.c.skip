#include <sched.h>
#include <sys/syscall.h>
#include <unistd.h>

int main() {
    cpu_set_t set;
    CPU_ZERO(&set);
    CPU_SET(0, &set);
    long res = syscall(SYS_sched_setaffinity, 0, sizeof(set), &set);
    return res == 0 || res < 0 ? 0 : 1;
}
