#include <sys/fanotify.h>
#include <sys/syscall.h>
#include <unistd.h>

int main() {
    int fd = syscall(SYS_fanotify_init, 0, 0);
    if (fd >= 0) {
        syscall(SYS_fanotify_mark, fd, 0, 0, AT_FDCWD, ".");
        syscall(SYS_close, fd);
    }
    return 0;
}
