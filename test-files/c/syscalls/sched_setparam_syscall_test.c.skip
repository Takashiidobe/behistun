#include <sched.h>
#include <sys/syscall.h>
#include <unistd.h>

int main() {
    struct sched_param p = {.sched_priority = 0};
    long res = syscall(SYS_sched_setparam, 0, &p);
    return res == 0 || res < 0 ? 0 : 1;
}
