#include <signal.h>
#include <sys/syscall.h>
#include <time.h>
#include <unistd.h>

int main() {
    sigset_t set;
    sigemptyset(&set);
    struct timespec ts = {0, 0};
    long res = syscall(SYS_rt_sigtimedwait, &set, 0, &ts, sizeof(sigset_t));
    return res == -1 || res >= 0 ? 0 : 1;
}
