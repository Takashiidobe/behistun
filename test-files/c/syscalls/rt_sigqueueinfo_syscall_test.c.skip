#include <signal.h>
#include <sys/syscall.h>
#include <unistd.h>

int main() {
    siginfo_t info;
    long res = syscall(SYS_rt_sigqueueinfo, getpid(), SIGUSR1, &info);
    return res == 0 || res < 0 ? 0 : 1;
}
