#include <mqueue.h>
#include <sys/syscall.h>
#include <time.h>
#include <unistd.h>

int main() {
    long res = syscall(SYS_mq_timedsend, -1, "x", 1, 0, (struct timespec *)0);
    (void)res;
    return 0;
}
